import { Alert } from "react-bootstrap";
import { AiFillCheckCircle } from "react-icons/ai";

export const CompletedCheck = () => {
	return <AiFillCheckCircle color="green" enableBackground="#fff" size={40  } />;
};
